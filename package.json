{
  "name": "studio",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "export": "turbo run export --filter=slides",
    "export:pptx": "turbo run export:pptx --filter=slides"
  },
  "devDependencies": {
    "turbo": "^2.8.21"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*"
  ]
}
